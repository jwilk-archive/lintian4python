# regexp-syntax-error 5 unbalanced parenthesis
# regexp-syntax-error 6 unexpected end of regular expression
# regexp-syntax-error 7 bad character range

re.compile('(a')
re.compile('[a')
re.compile('[z-a]')

# vim: syntax=python sw=4 sts=4 sr et
